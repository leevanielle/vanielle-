---
import Layout from "@layouts/Layout.astro";
import NewsletterSignup from "@components/NewsletterSignup.astro";

const AMAZON_URL = "https://www.amazon.com/dp/B0HF6JW33T";
const LINKEDIN_URL = "https://linkedin.com/in/vanielle";
const FORBES_URL =
  "https://www.forbes.com/sites/jonyounger/2020/11/23/meet-another-freelance-innovator-odecloud-a-netsuite-expert-community/";

const expertise = [
  "Agentic AI and LLM systems",
  "Full-stack product engineering",
  "Cloud, infrastructure, and data",
  "Technical strategy and team leadership",
];

const links = [
  {
    label: "X / Twitter",
    handle: "@lee_vanielle",
    href: "https://x.com/lee_vanielle",
  },
  {
    label: "LinkedIn",
    handle: "/in/vanielle",
    href: LINKEDIN_URL,
  },
  {
    label: "GitHub",
    handle: "github.com/leevanielle",
    href: "https://github.com/leevanielle",
  },
  {
    label: "Stan Store",
    handle: "stan.store/vanielle",
    href: "https://stan.store/vanielle",
  },
];
---

<Layout
  title="Vanielle Lee — AI Engineer, Founder & CTO"
  description="AI engineer, founder, and OdeCloud CTO with 10+ years building products, platforms, teams, and agentic AI systems."
>
  <div class="site-shell">
    <div class="opening-sequence" aria-hidden="true">
      <div class="shutter shutter-left"></div>
      <div class="shutter shutter-right"></div>
      <div class="opening-slate">
        <span>AI engineer · Founder · CTO</span>
        <strong>Vanielle Lee</strong>
        <i>Entering operator mode…</i>
      </div>
    </div>

    <div class="scroll-progress" aria-hidden="true">
      <span class="scroll-progress-fill"></span>
    </div>

    <header class="mission-nav">
      <a class="wordmark" href="#opening" aria-label="Vanielle Lee, home">
        <span>Vanielle</span><i aria-hidden="true">■</i><span>Lee</span>
      </a>

      <nav aria-label="Chapter navigation">
        <a href="#work" data-chapter-link="work">
          <small>01</small><span>Experience</span>
        </a>
        <a href="#book" data-chapter-link="book">
          <small>02</small><span>Book</span>
        </a>
        <a href="#notes" data-chapter-link="notes">
          <small>03</small><span>Field notes</span>
        </a>
      </nav>

      <a class="nav-cta" href="#newsletter" data-focus-newsletter>
        Join field notes <span aria-hidden="true">↘</span>
      </a>
    </header>

    <main>
      <section
        class="hero-cinematic"
        id="opening"
        data-chapter="opening"
        aria-labelledby="hero-heading"
      >
        <div class="hero-marquee" aria-hidden="true">
          Code · Product · Teams · Company · AI · Code · Product ·
        </div>

        <div class="hero-stage">
          <div class="hero-copy">
            <p class="hero-credit">
              <span>Operator profile 001</span>
              <span>10+ years · Full stack</span>
            </p>

            <h1 id="hero-heading">
              <span class="title-primary">I build AI systems—</span>
              <span class="title-secondary">and the companies</span>
              <span class="title-accent">around them.</span>
            </h1>

            <p class="hero-intro">
              AI engineer, founder, and OdeCloud CTO with 10+
              years across the full stack—from frontend and infrastructure to
              data and agentic AI. I turn complex engineering into products,
              teams, and business momentum.
            </p>

            <div class="hero-signup" id="newsletter">
              <div class="signup-promise">
                <span class="signal-dot" aria-hidden="true"></span>
                <p>
                  <strong>Notes from the codebase and the boardroom.</strong>
                   Practical AI, the unspoken hiring playbook, industry
                  shortcuts, and candid cross-cultural founder notes.
                </p>
              </div>
              <NewsletterSignup />
            </div>

            <div class="hero-secondary">
              <a href="#consulting">
                Work with me · from $300/hr <span aria-hidden="true">↓</span>
              </a>
              <a href={FORBES_URL} target="_blank" rel="noopener noreferrer">
                OdeCloud in Forbes <span aria-hidden="true">↗</span>
              </a>
            </div>
          </div>

          <figure class="portrait-stage" data-tilt>
            <div class="frame-index">
              <span>Frame 00:01:27</span>
              <span>AI engineer / Co-founder / CTO</span>
            </div>
            <div class="portrait-window">
              <img
                src="assets/vanielle-profile.jpg"
                width="894"
                height="547"
                alt="Vanielle Lee smiling in a dark turtleneck"
                loading="eager"
                fetchpriority="high"
              />
              <span class="scan-line" aria-hidden="true"></span>
              <span class="target target-a" aria-hidden="true"></span>
              <span class="target target-b" aria-hidden="true"></span>
            </div>
            <figcaption>
              <span>10+ years in engineering</span>
              <strong>8 years as CTO</strong>
            </figcaption>
          </figure>
        </div>

        <a class="scroll-cue" href="#work">
          <span>Scroll to continue</span>
          <i aria-hidden="true"></i>
        </a>
      </section>

      <section class="proof-ribbon" aria-label="Career proof points">
        <div>
          <strong>10+</strong>
          <span>years in engineering</span>
        </div>
        <div>
          <strong>8</strong>
          <span>years as CTO</span>
        </div>
        <a href={FORBES_URL} target="_blank" rel="noopener noreferrer">
          <strong>Forbes</strong>
          <span>OdeCloud profile ↗</span>
        </a>
        <div>
          <strong>Enterprise</strong>
          <span>JVC Kenwood · Coinbase · WarnerMedia</span>
        </div>
      </section>

      <section class="offer-chapter" aria-labelledby="offer-heading">
        <div class="offer-heading" data-reveal="left">
          <p class="eyebrow">Three ways in</p>
          <h2 id="offer-heading">Choose your next move.</h2>
          <p>
            Bring me the hard decision, read the field manual, or get the next
            useful idea in your inbox.
          </p>
        </div>

        <div class="offer-grid">
          <article id="consulting" data-reveal="right">
            <div class="offer-index">
              <span>01 / Advisory</span>
              <i>Direct access</i>
            </div>
            <h3>Talk through the problem with me.</h3>
            <p>
              For founders and leaders facing a consequential AI, engineering,
              product, or team decision.
            </p>
            <div class="offer-price">
              <strong>$300 / hour</strong>
              <span>Starting rate · projects scoped separately</span>
            </div>
            <a href={LINKEDIN_URL} target="_blank" rel="noopener noreferrer">
              Start a conversation <span aria-hidden="true">↗</span>
            </a>
          </article>

          <article data-reveal="up">
            <div class="offer-index">
              <span>02 / The book</span>
              <i>Read now</i>
            </div>
            <h3>Lead engineering without becoming the hero.</h3>
            <p>
              Read <cite>Emperors, Prophets, and Product Managers</cite>—my
              field guide to engineering leadership, management, and recurring
              organizational chaos.
            </p>
            <a href={AMAZON_URL} target="_blank" rel="noopener noreferrer">
              Get the book <span aria-hidden="true">↗</span>
            </a>
          </article>

          <article class="offer-newsletter" data-reveal="left">
            <div class="offer-index">
              <span>03 / Field notes</span>
              <i>Free</i>
            </div>
            <h3>Get the playbook as I learn it.</h3>
            <p>
              Practical AI tips, industry shortcuts, getting hired, and candid
              dispatches from building a company across cultures.
            </p>
            <a href="#newsletter" data-focus-newsletter>
              Join the newsletter <span aria-hidden="true">↗</span>
            </a>
          </article>
        </div>
      </section>

      <section
        class="work-chapter"
        id="work"
        data-chapter="work"
        aria-labelledby="work-heading"
      >
        <div class="chapter-register chapter-register-dark" aria-hidden="true">
          <span>Act 01 / Experience</span>
          <span>Code · company · people</span>
        </div>

        <div class="work-layout">
          <div class="work-heading" data-reveal="left">
            <p class="eyebrow">The operating range</p>
            <h2 id="work-heading">From the codebase to the boardroom.</h2>
          </div>

          <div class="work-story" data-reveal="right">
            <p class="lead">
              I started as the engineer who could work anywhere in the stack.
              Over 10+ years, the scope expanded: product, hiring, strategy,
              leadership—and now agentic AI.
            </p>
            <p>
              At OdeCloud, I’ve spent eight years as CTO,
              translating what the business needs into systems teams can
              actually build, operate, and grow.
            </p>
            <div class="advisory-callout">
              <span>The throughline</span>
              <p>
                Make complex technology legible. Give technical teams enough
                context to make good decisions—and business leaders enough
                clarity to trust them.
              </p>
            </div>
            <p class="enterprise-proof">
              OdeCloud has delivered work for enterprise teams including JVC
              Kenwood, Coinbase, and WarnerMedia.
              <a href={FORBES_URL} target="_blank" rel="noopener noreferrer">
                Read the OdeCloud Forbes profile ↗
              </a>
            </p>
          </div>

          <div class="expertise-card" data-reveal="up">
            <p>Experience / current</p>
            <ul>{expertise.map(item => <li>{item}</li>)}</ul>
          </div>
        </div>
      </section>

      <section
        class="book-chapter"
        id="book"
        data-chapter="book"
        aria-labelledby="book-heading"
      >
        <div class="chapter-register" aria-hidden="true">
          <span>Act 02 / The book</span>
          <span>Prophet’s guides — field manual № 001</span>
        </div>

        <div class="book-layout">
          <figure class="book-stage" data-reveal="left">
            <div class="book-shadow" aria-hidden="true"></div>
            <div class="book-frame">
              <img
                src="assets/book-cover.jpg?v=3"
                width="1600"
                height="2560"
                alt="Cover of Emperors, Prophets, and Product Managers by Vanielle Lee"
                loading="lazy"
              />
            </div>
            <figcaption>Cover artwork · final edition</figcaption>
          </figure>

          <div class="book-copy" data-reveal="right">
            <p class="eyebrow">The new book</p>
            <h2 id="book-heading">
              Leadership has always been chaos.
              <em>The tools just changed.</em>
            </h2>
            <p class="book-lede">
              <cite>Emperors, Prophets, and Product Managers</cite> is a CTO’s
              guide to the patterns that keep resurfacing wherever power,
              prediction, technology, and human judgment meet.
            </p>
            <div class="book-stats" aria-label="Book details">
              <span><strong>12</strong> chapters</span>
              <span><strong>6</strong> frameworks</span>
              <span><strong>1</strong> honest field guide</span>
            </div>
            <a
              class="button button-book"
              href={AMAZON_URL}
              target="_blank"
              rel="noopener noreferrer"
            >
              Get the book <span aria-hidden="true">↗</span>
            </a>
          </div>
        </div>
      </section>

      <section
        class="notes-chapter"
        id="notes"
        data-chapter="notes"
        aria-labelledby="notes-heading"
      >
        <div class="notes-heading" data-reveal="up">
          <p class="eyebrow eyebrow-light">Act 03 / Field notes</p>
          <h2 id="notes-heading">
            Ideas you can use—even if you don’t write code.
          </h2>
          <p>
            I explain what technology means for the people funding it, building
            it, and living with the outcome.
          </p>
        </div>

        <div class="dispatch-list">
          <article data-reveal="left">
            <span>01</span>
            <div>
              <h3>Practical AI, without the theater</h3>
              <p>
                What agentic systems can do, where they fail, and what it takes
                to ship them.
              </p>
            </div>
          </article>
          <article data-reveal="right">
            <span>02</span>
            <div>
              <h3>The unspoken career playbook</h3>
              <p>
                Industry shortcuts, getting hired, and the choices that create
                real career leverage.
              </p>
            </div>
          </article>
          <article data-reveal="left">
            <span>03</span>
            <div>
              <h3>Cross-cultural founder notes</h3>
              <p>
                Candid lessons from building and leading a company across
                countries, expectations, and working styles.
              </p>
            </div>
          </article>
        </div>

        <a class="notes-cta" href="#newsletter" data-focus-newsletter>
          <span>Open the channel</span>
          <strong>Send me the next field note</strong>
          <i aria-hidden="true">↗</i>
        </a>
      </section>

      <section
        class="connect-chapter"
        id="connect"
        data-chapter="connect"
        aria-labelledby="connect-heading"
      >
        <div data-reveal="left">
          <p class="eyebrow eyebrow-light">End credits / Let’s talk</p>
          <h2 id="connect-heading">Bring me the hard decision.</h2>
          <p class="connect-lede">
            Advisory sessions start at $300/hour. For projects, speaking, or an
            ongoing engagement, start with a message on LinkedIn.
          </p>
        </div>

        <div class="link-grid" data-reveal="right">
          {
            links.map((link, index) => (
              <a href={link.href} target="_blank" rel="noopener noreferrer">
                <small>{String(index + 1).padStart(2, "0")}</small>
                <span>
                  <strong>{link.label}</strong>
                  <i>{link.handle}</i>
                </span>
                <b aria-hidden="true">↗</b>
              </a>
            ))
          }
        </div>
      </section>
    </main>

    <footer>
      <p>AI engineer · Co-founder · CTO</p>
      <a href="#newsletter" data-focus-newsletter>Join the field notes ↑</a>
      <p>© {new Date().getFullYear()} Vanielle Lee</p>
    </footer>
  </div>
</Layout>

<script>
  const shell = document.querySelector<HTMLElement>(".site-shell");

  if (shell && shell.dataset.motionReady !== "true") {
    shell.dataset.motionReady = "true";
    const reduceMotion = window.matchMedia(
      "(prefers-reduced-motion: reduce)"
    ).matches;

    if (!reduceMotion) {
      shell.classList.add("motion-ready");
      const reveals = document.querySelectorAll<HTMLElement>("[data-reveal]");
      const revealObserver = new IntersectionObserver(
        entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              revealObserver.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.08, rootMargin: "0px 0px -12% 0px" }
      );
      reveals.forEach(element => revealObserver.observe(element));
    }

    const chapterLinks = document.querySelectorAll<HTMLAnchorElement>(
      "[data-chapter-link]"
    );
    const chapters = document.querySelectorAll<HTMLElement>("[data-chapter]");
    const chapterObserver = new IntersectionObserver(
      entries => {
        const current = entries
          .filter(entry => entry.isIntersecting)
          .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
        if (!current) return;

        const chapter =
          current.target instanceof HTMLElement
            ? current.target.dataset.chapter
            : undefined;
        chapterLinks.forEach(link => {
          if (link.dataset.chapterLink === chapter) {
            link.setAttribute("aria-current", "location");
          } else {
            link.removeAttribute("aria-current");
          }
        });
      },
      { threshold: 0.01, rootMargin: "-45% 0px -45% 0px" }
    );
    chapters.forEach(chapter => chapterObserver.observe(chapter));

    let progressFrame = 0;
    const revealVisible = () => {
      if (reduceMotion) return;
      document
        .querySelectorAll<HTMLElement>("[data-reveal]")
        .forEach(element => {
          const bounds = element.getBoundingClientRect();
          if (bounds.top < window.innerHeight * 0.88 && bounds.bottom > 0) {
            element.classList.add("is-visible");
          }
        });
    };
    const updateProgress = () => {
      const max = document.documentElement.scrollHeight - window.innerHeight;
      const ratio = max > 0 ? window.scrollY / max : 0;
      shell.style.setProperty("--scroll-progress", `${ratio * 100}%`);
      revealVisible();
      progressFrame = 0;
    };
    window.addEventListener(
      "scroll",
      () => {
        if (!progressFrame) {
          progressFrame = window.requestAnimationFrame(updateProgress);
        }
      },
      { passive: true }
    );
    updateProgress();

    document
      .querySelectorAll<HTMLAnchorElement>("[data-focus-newsletter]")
      .forEach(link => {
        link.addEventListener("click", () => {
          window.setTimeout(
            () => {
              document
                .querySelector<HTMLInputElement>("#newsletter-email")
                ?.focus({ preventScroll: true });
            },
            reduceMotion ? 0 : 650
          );
        });
      });

    const portrait = document.querySelector<HTMLElement>("[data-tilt]");
    if (portrait && !reduceMotion) {
      portrait.addEventListener("pointermove", event => {
        const bounds = portrait.getBoundingClientRect();
        const x = (event.clientX - bounds.left) / bounds.width - 0.5;
        const y = (event.clientY - bounds.top) / bounds.height - 0.5;
        portrait.style.setProperty("--tilt-x", `${x * 3.5}deg`);
        portrait.style.setProperty("--tilt-y", `${y * -3.5}deg`);
      });
      portrait.addEventListener("pointerleave", () => {
        portrait.style.setProperty("--tilt-x", "0deg");
        portrait.style.setProperty("--tilt-y", "0deg");
      });
    }
  }
</script>

<style>
  :global(html) {
    scroll-behavior: smooth;
    scroll-padding-top: 5rem;
  }
  :global(body) {
    margin: 0;
    background: #11110f;
    color: #191915;
    font-family: "Instrument Sans", Inter, ui-sans-serif, system-ui, sans-serif;
  }
  :global(*) {
    box-sizing: border-box;
  }

  .site-shell {
    --ink: #171713;
    --ink-soft: #24231f;
    --paper: #f1ecdf;
    --paper-bright: #fbf8ef;
    --red: #cf2f21;
    --red-bright: #e63b2c;
    --amber: #d9a738;
    --salmon: #ef746d;
    --muted: #777064;
    --scroll-progress: 0%;
    min-height: 100vh;
    overflow: clip;
    background: var(--ink);
  }
  .site-shell > main {
    width: 100%;
    max-width: none;
    margin: 0;
    padding: 0;
  }
  .site-shell main > section,
  .site-shell > footer {
    width: 100%;
    max-width: none;
    margin: 0;
  }

  .opening-sequence {
    position: fixed;
    inset: 0;
    z-index: 100;
    pointer-events: none;
    animation: sequence-exit 1ms linear 1.72s forwards;
  }
  .shutter {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 51%;
    background: linear-gradient(rgba(25, 25, 21, 0.045) 1px, transparent 1px),
      var(--paper);
    background-size: 100% 30px;
  }
  .shutter::after {
    content: "";
    position: absolute;
    top: 2rem;
    bottom: 2rem;
    width: 1px;
    background: var(--ink);
  }
  .shutter-left {
    left: 0;
    animation: shutter-left 900ms cubic-bezier(0.78, 0, 0.2, 1) 0.72s forwards;
  }
  .shutter-left::after {
    right: 2rem;
  }
  .shutter-right {
    right: 0;
    animation: shutter-right 900ms cubic-bezier(0.78, 0, 0.2, 1) 0.72s forwards;
  }
  .shutter-right::after {
    left: 2rem;
  }
  .opening-slate {
    position: absolute;
    top: 50%;
    left: 50%;
    width: min(540px, calc(100% - 3rem));
    padding: 1.4rem;
    display: grid;
    gap: 0.55rem;
    border: 1px solid var(--ink);
    background: var(--paper-bright);
    color: var(--ink);
    transform: translate(-50%, -50%);
    text-align: center;
    text-transform: uppercase;
    animation: slate-out 380ms ease 0.62s forwards;
  }
  .opening-slate span,
  .opening-slate i {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.63rem;
    font-style: normal;
    letter-spacing: 0.12em;
  }
  .opening-slate strong {
    color: var(--red);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 500;
    letter-spacing: -0.04em;
  }

  .scroll-progress {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 80;
    height: 3px;
    background: rgba(255, 255, 255, 0.13);
  }
  .scroll-progress-fill {
    width: var(--scroll-progress);
    height: 100%;
    display: block;
    background: var(--red-bright);
    transition: width 70ms linear;
  }

  .mission-nav {
    position: fixed;
    top: 3px;
    left: 0;
    right: 0;
    z-index: 70;
    min-height: 72px;
    padding: 0 max(1rem, calc((100vw - 1280px) / 2));
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: clamp(1rem, 4vw, 4rem);
    border-bottom: 1px solid rgba(242, 238, 228, 0.19);
    background: rgba(17, 17, 15, 0.88);
    color: var(--paper-bright);
    backdrop-filter: blur(14px);
  }
  .wordmark {
    display: flex;
    align-items: center;
    color: inherit;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: -0.04em;
    text-decoration: none;
    text-transform: uppercase;
  }
  .wordmark i {
    margin-inline: 0.48rem;
    color: var(--red-bright);
    font-size: 0.62em;
    font-style: normal;
  }
  .mission-nav nav {
    justify-self: end;
    display: flex;
    gap: clamp(0.8rem, 2vw, 2rem);
  }
  .mission-nav nav a {
    position: relative;
    padding: 1.65rem 0.1rem 1.4rem;
    display: flex;
    align-items: baseline;
    gap: 0.45rem;
    color: #aaa59a;
    font-size: 0.76rem;
    font-weight: 650;
    text-decoration: none;
  }
  .mission-nav nav a::after {
    content: "";
    position: absolute;
    left: 0;
    right: 100%;
    bottom: -1px;
    height: 3px;
    background: var(--red-bright);
    transition: right 240ms ease;
  }
  .mission-nav nav a:hover,
  .mission-nav nav a[aria-current="location"] {
    color: var(--paper-bright);
  }
  .mission-nav nav a[aria-current="location"]::after {
    right: 0;
  }
  .mission-nav nav small {
    color: var(--red-bright);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.54rem;
  }
  .nav-cta {
    min-height: 42px;
    padding: 0.65rem 0.85rem;
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    border: 1px solid var(--red-bright);
    background: var(--red);
    color: white;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.67rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    transition:
      background 180ms ease,
      transform 180ms ease;
  }
  .nav-cta:hover {
    background: var(--red-bright);
    transform: translateY(-2px);
  }

  .hero-cinematic {
    position: relative;
    min-height: max(780px, 100svh);
    padding: clamp(7.5rem, 12vh, 10rem) max(1rem, calc((100vw - 1280px) / 2))
      5rem;
    display: flex;
    align-items: center;
    overflow: hidden;
    background: radial-gradient(
        circle at 74% 45%,
        rgba(217, 167, 56, 0.08),
        transparent 30%
      ),
      linear-gradient(115deg, #151511 0 62%, #0c0c0b 62% 100%);
    color: var(--paper-bright);
  }
  .hero-cinematic::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: repeating-linear-gradient(
      0deg,
      transparent 0 30px,
      rgba(255, 255, 255, 0.024) 30px 31px
    );
  }
  .hero-marquee {
    position: absolute;
    left: -4vw;
    bottom: -0.19em;
    z-index: 0;
    width: max-content;
    color: rgba(242, 238, 228, 0.035);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(6rem, 15vw, 13rem);
    font-style: italic;
    line-height: 0.75;
    white-space: nowrap;
    animation: marquee-drift 28s linear infinite alternate;
  }
  .hero-stage {
    position: relative;
    z-index: 2;
    width: min(1280px, 100%);
    margin-inline: auto;
    display: grid;
    grid-template-columns: minmax(0, 1.14fr) minmax(360px, 0.86fr);
    align-items: center;
    gap: clamp(3rem, 7vw, 7rem);
  }
  .hero-copy {
    min-width: 0;
  }
  .hero-credit {
    margin: 0 0 1.2rem;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: var(--amber);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.63rem;
    font-weight: 650;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }
  h1,
  h2 {
    margin: 0;
    font-family: "Bodoni Moda", Didot, Georgia, serif;
    font-weight: 500;
    text-wrap: balance;
  }
  h1 {
    max-width: 780px;
    font-size: clamp(4rem, 5.5vw, 5.8rem);
    letter-spacing: -0.075em;
    line-height: 0.84;
  }
  h1 span {
    display: block;
  }
  h1 .title-accent {
    color: var(--salmon);
    font-style: italic;
  }
  .hero-intro {
    max-width: 690px;
    margin: 1.6rem 0 0;
    color: #c5c0b6;
    font-size: clamp(1rem, 1.5vw, 1.14rem);
    line-height: 1.65;
  }
  .hero-signup {
    max-width: 720px;
    margin-top: 1.55rem;
    padding: 1rem 1rem 1.1rem;
    border: 1px solid #5f5b52;
    background: rgba(251, 248, 239, 0.055);
    box-shadow: 9px 9px 0 var(--red);
  }
  .signup-promise {
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: #d4cfc4;
  }
  .signup-promise p {
    margin: 0;
    font-size: 0.82rem;
    line-height: 1.45;
  }
  .signup-promise strong {
    color: var(--paper-bright);
  }
  .signal-dot {
    width: 9px;
    height: 9px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--salmon);
    box-shadow: 0 0 0 5px rgba(239, 116, 109, 0.16);
    animation: signal-pulse 1.8s ease-in-out infinite;
  }
  .hero-secondary {
    margin-top: 1.2rem;
    display: flex;
    gap: 1.6rem;
    flex-wrap: wrap;
  }
  .hero-secondary a {
    color: #d0cbc1;
    font-size: 0.78rem;
    font-weight: 700;
    text-decoration-color: var(--red-bright);
    text-underline-offset: 5px;
  }
  .hero-secondary a:hover {
    color: white;
  }

  .portrait-stage {
    --tilt-x: 0deg;
    --tilt-y: 0deg;
    position: relative;
    margin: 0;
    padding: 1rem;
    border: 1px solid #5d584f;
    background: #0f0f0d;
    box-shadow:
      14px 14px 0 var(--amber),
      -14px -14px 0 rgba(207, 47, 33, 0.7);
    transform: perspective(1100px) rotateX(var(--tilt-y)) rotateY(var(--tilt-x));
    transform-style: preserve-3d;
    transition: transform 180ms ease-out;
  }
  .frame-index,
  .portrait-stage figcaption {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: #bbb6ad;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.57rem;
    letter-spacing: 0.055em;
    text-transform: uppercase;
  }
  .frame-index {
    padding: 0 0.15rem 0.8rem;
  }
  .portrait-window {
    position: relative;
    overflow: hidden;
    border: 1px solid #747067;
  }
  .portrait-window img {
    width: 100%;
    height: auto;
    display: block;
    filter: grayscale(1) contrast(1.08);
    transform: scale(1.015);
  }
  .portrait-window::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(
        90deg,
        rgba(239, 116, 109, 0.16) 1px,
        transparent 1px
      ),
      linear-gradient(rgba(239, 116, 109, 0.13) 1px, transparent 1px);
    background-size:
      25% 100%,
      100% 33.333%;
    opacity: 0.25;
  }
  .scan-line {
    position: absolute;
    left: 0;
    right: 0;
    top: -8%;
    z-index: 2;
    height: 2px;
    background: var(--salmon);
    box-shadow: 0 0 18px 4px rgba(239, 116, 109, 0.55);
    animation: scan 4.8s linear infinite;
  }
  .target {
    position: absolute;
    z-index: 3;
    width: 20px;
    height: 20px;
    border-color: var(--salmon);
  }
  .target-a {
    top: 7%;
    left: 6%;
    border-top: 2px solid var(--salmon);
    border-left: 2px solid var(--salmon);
  }
  .target-b {
    right: 6%;
    bottom: 7%;
    border-right: 2px solid var(--salmon);
    border-bottom: 2px solid var(--salmon);
  }
  .portrait-stage figcaption {
    padding: 0.85rem 0.15rem 0;
  }
  .portrait-stage figcaption strong {
    color: var(--amber);
    font-weight: 650;
  }
  .scroll-cue {
    position: absolute;
    left: 50%;
    bottom: 1.3rem;
    z-index: 3;
    display: grid;
    justify-items: center;
    gap: 0.45rem;
    color: #8f8a81;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.55rem;
    letter-spacing: 0.08em;
    text-decoration: none;
    text-transform: uppercase;
    transform: translateX(-50%);
  }
  .scroll-cue i {
    width: 1px;
    height: 28px;
    display: block;
    background: var(--red-bright);
    transform-origin: top;
    animation: scroll-line 1.8s ease-in-out infinite;
  }

  .proof-ribbon {
    padding-inline: max(1rem, calc((100vw - 1280px) / 2));
    display: grid;
    grid-template-columns: 0.72fr 0.82fr 0.86fr 1.6fr;
    border-block: 1px solid var(--ink);
    background: var(--paper-bright);
    color: var(--ink);
  }
  .proof-ribbon > * {
    min-width: 0;
    min-height: 118px;
    padding: 1.2rem clamp(1rem, 2vw, 1.6rem);
    display: flex;
    flex-direction: column;
    justify-content: center;
    border-left: 1px solid #8e887c;
  }
  .proof-ribbon > *:last-child {
    border-right: 1px solid #8e887c;
  }
  .proof-ribbon strong {
    display: block;
    color: var(--red);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(1.65rem, 2.5vw, 2.45rem);
    font-weight: 500;
    letter-spacing: -0.045em;
    line-height: 1;
  }
  .proof-ribbon span {
    margin-top: 0.45rem;
    color: #5c574e;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.58rem;
    line-height: 1.45;
    text-transform: uppercase;
  }
  .proof-ribbon a {
    color: inherit;
    text-decoration: none;
    transition:
      background 180ms ease,
      color 180ms ease;
  }
  .proof-ribbon a:hover {
    background: var(--red);
  }
  .proof-ribbon a:hover strong,
  .proof-ribbon a:hover span {
    color: white;
  }

  .offer-chapter {
    padding: clamp(6.5rem, 10vw, 8.5rem) max(1rem, calc((100vw - 1160px) / 2));
    background: var(--paper);
    color: var(--ink);
  }
  .offer-heading {
    margin-bottom: clamp(2.5rem, 5vw, 4.5rem);
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    align-items: end;
    gap: clamp(2rem, 6vw, 6rem);
  }
  .offer-heading .eyebrow {
    grid-column: 1 / -1;
    margin-bottom: -1.6rem;
  }
  .offer-heading h2 {
    max-width: 700px;
    font-size: clamp(3.6rem, 6.3vw, 6.4rem);
    letter-spacing: -0.065em;
    line-height: 0.88;
  }
  .offer-heading > p:last-child {
    max-width: 470px;
    margin: 0;
    color: #5e594f;
    font-size: 1.02rem;
    line-height: 1.65;
  }
  .offer-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid var(--ink);
    border-left: 1px solid var(--ink);
  }
  .offer-grid article {
    min-width: 0;
    min-height: 440px;
    padding: clamp(1.3rem, 2.4vw, 2rem);
    display: flex;
    flex-direction: column;
    border-right: 1px solid var(--ink);
    border-bottom: 1px solid var(--ink);
  }
  .offer-grid article:first-child {
    background: var(--ink);
    color: var(--paper-bright);
  }
  .offer-grid article.offer-newsletter {
    background: var(--red);
    color: white;
  }
  .offer-index {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.58rem;
    font-weight: 650;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  .offer-index span {
    color: var(--red);
  }
  .offer-index i {
    color: #736d62;
    font-style: normal;
  }
  .offer-grid article:first-child .offer-index span,
  .offer-grid article.offer-newsletter .offer-index span {
    color: var(--amber);
  }
  .offer-grid article:first-child .offer-index i,
  .offer-grid article.offer-newsletter .offer-index i {
    color: #c6c0b5;
  }
  .offer-grid h3 {
    margin: clamp(2.3rem, 4vw, 3.4rem) 0 1.2rem;
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(2.1rem, 3.2vw, 3.1rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 0.98;
  }
  .offer-grid article > p {
    margin: 0;
    color: #5e594f;
    font-size: 0.88rem;
    line-height: 1.65;
  }
  .offer-grid article:first-child > p {
    color: #c4bfb4;
  }
  .offer-grid article.offer-newsletter > p {
    color: #f7d9d3;
  }
  .offer-price {
    margin: 1.7rem 0 0;
    padding-top: 1.2rem;
    border-top: 1px solid #5d584f;
  }
  .offer-price strong,
  .offer-price span {
    display: block;
  }
  .offer-price strong {
    color: var(--amber);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: 2rem;
    font-weight: 500;
    letter-spacing: -0.04em;
  }
  .offer-price span {
    margin-top: 0.35rem;
    color: #aaa49a;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.53rem;
    line-height: 1.45;
    text-transform: uppercase;
  }
  .offer-grid article > a {
    margin-top: auto;
    padding-top: 1.4rem;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    border-top: 1px solid currentColor;
    color: inherit;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.66rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
  }
  .offer-grid article > a:hover {
    color: var(--red);
  }
  .offer-grid article:first-child > a:hover,
  .offer-grid article.offer-newsletter > a:hover {
    color: var(--amber);
  }

  .chapter-register {
    position: absolute;
    top: 2rem;
    left: max(1rem, calc((100vw - 1160px) / 2));
    right: max(1rem, calc((100vw - 1160px) / 2));
    padding-bottom: 1rem;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    border-bottom: 1px solid #8e887c;
    color: #726c61;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.59rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }
  .chapter-register-dark {
    border-color: #777166;
    color: #a49e93;
  }
  .eyebrow {
    margin: 0 0 1.1rem;
    color: var(--red);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .eyebrow-light {
    color: var(--amber);
  }

  .book-chapter {
    position: relative;
    min-height: 100svh;
    padding: clamp(7.5rem, 12vw, 10rem) max(1rem, calc((100vw - 1160px) / 2));
    display: flex;
    align-items: center;
    overflow: hidden;
    background: linear-gradient(rgba(25, 25, 21, 0.038) 1px, transparent 1px),
      var(--paper);
    background-size: 100% 29px;
  }
  .book-layout {
    width: min(1160px, 100%);
    margin-inline: auto;
    display: grid;
    grid-template-columns: minmax(300px, 0.78fr) minmax(0, 1.22fr);
    align-items: center;
    gap: clamp(4rem, 9vw, 8rem);
  }
  .book-stage {
    position: relative;
    margin: 0;
  }
  .book-shadow {
    position: absolute;
    inset: 5% -7% -4% 9%;
    background: var(--red);
    transform: rotate(2deg);
  }
  .book-frame {
    position: relative;
    padding: 0.55rem;
    border: 1px solid var(--ink);
    background: #d5cfc1;
    transform: rotate(-1.4deg);
    transition: transform 300ms cubic-bezier(0.2, 0.8, 0.2, 1);
  }
  .book-frame:hover {
    transform: rotate(0) translateY(-8px);
  }
  .book-frame img {
    width: 100%;
    height: auto;
    display: block;
  }
  .book-stage figcaption {
    position: relative;
    margin-top: 1.25rem;
    color: #726c61;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.58rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }
  .book-copy {
    position: relative;
  }
  .book-copy h2 {
    max-width: 720px;
    font-size: clamp(3.5rem, 6.2vw, 6.3rem);
    letter-spacing: -0.065em;
    line-height: 0.89;
  }
  .book-copy h2 em {
    display: block;
    color: var(--red);
    font-weight: 500;
  }
  .book-lede {
    max-width: 650px;
    margin: 1.8rem 0 0;
    color: #504c43;
    font-size: clamp(1rem, 1.4vw, 1.13rem);
    line-height: 1.72;
  }
  .book-lede cite {
    color: var(--ink);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: 1.16em;
  }
  .book-stats {
    margin: 2rem 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid var(--ink);
    border-left: 1px solid var(--ink);
  }
  .book-stats span {
    min-height: 90px;
    padding: 1rem;
    display: grid;
    align-content: center;
    border-right: 1px solid var(--ink);
    border-bottom: 1px solid var(--ink);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.61rem;
    text-transform: uppercase;
  }
  .book-stats strong {
    margin-bottom: 0.2rem;
    color: var(--red);
    font-family: "Bodoni Moda", Didot, serif;
    font-size: 2.2rem;
    font-weight: 500;
    line-height: 1;
  }
  .button {
    min-height: 52px;
    padding: 0.85rem 1.15rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    border: 1px solid var(--ink);
    background: var(--ink);
    color: white;
    box-shadow: 7px 7px 0 var(--amber);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.72rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    transition:
      transform 170ms ease,
      box-shadow 170ms ease;
  }
  .button:hover {
    box-shadow: 10px 10px 0 var(--amber);
    transform: translate(-2px, -2px);
  }

  .notes-chapter {
    position: relative;
    min-height: 100svh;
    padding: clamp(7rem, 12vw, 10rem) max(1rem, calc((100vw - 1160px) / 2));
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    align-content: center;
    gap: clamp(3rem, 8vw, 7rem);
    background: linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px),
      var(--red);
    background-size: 100% 29px;
    color: var(--paper-bright);
  }
  .notes-heading h2 {
    max-width: 540px;
    font-size: clamp(3.5rem, 6vw, 6rem);
    letter-spacing: -0.065em;
    line-height: 0.9;
  }
  .notes-heading > p:last-child {
    max-width: 520px;
    color: #f7d9d3;
    font-size: 1.05rem;
    line-height: 1.65;
  }
  .dispatch-list {
    border-top: 1px solid rgba(255, 255, 255, 0.65);
  }
  .dispatch-list article {
    padding: 1.45rem 0;
    display: grid;
    grid-template-columns: 55px 1fr;
    gap: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.65);
  }
  .dispatch-list article > span {
    color: var(--amber);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.65rem;
  }
  .dispatch-list h3 {
    margin: 0;
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(1.65rem, 2.7vw, 2.4rem);
    font-weight: 500;
    line-height: 1.1;
  }
  .dispatch-list p {
    margin: 0.45rem 0 0;
    color: #f7d9d3;
    line-height: 1.55;
  }
  .notes-cta {
    grid-column: 1 / -1;
    min-height: 118px;
    margin-top: 1rem;
    padding: 1.25rem 1.5rem;
    display: grid;
    grid-template-columns: 0.55fr 1.45fr auto;
    align-items: center;
    gap: 1.5rem;
    border: 1px solid var(--ink);
    background: var(--paper-bright);
    color: var(--ink);
    box-shadow: 10px 10px 0 var(--amber);
    text-decoration: none;
    transition:
      transform 200ms ease,
      box-shadow 200ms ease;
  }
  .notes-cta:hover {
    box-shadow: 14px 14px 0 var(--amber);
    transform: translate(-3px, -3px);
  }
  .notes-cta span {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.62rem;
    text-transform: uppercase;
  }
  .notes-cta strong {
    font-family: "Bodoni Moda", Didot, serif;
    font-size: clamp(1.8rem, 3.2vw, 3.1rem);
    font-weight: 500;
    letter-spacing: -0.045em;
  }
  .notes-cta i {
    color: var(--red);
    font-size: 2rem;
    font-style: normal;
  }

  .work-chapter {
    position: relative;
    min-height: 100svh;
    padding: clamp(8rem, 13vw, 11rem) max(1rem, calc((100vw - 1160px) / 2));
    background: var(--ink);
    color: var(--paper-bright);
  }
  .work-layout {
    width: min(1160px, 100%);
    margin-inline: auto;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: clamp(3rem, 7vw, 7rem);
  }
  .work-heading h2 {
    max-width: 680px;
    font-size: clamp(3.3rem, 5.6vw, 5.7rem);
    letter-spacing: -0.06em;
    line-height: 0.92;
  }
  .work-story {
    color: #c4bfb4;
    line-height: 1.7;
  }
  .work-story .lead {
    margin-top: 0;
    color: var(--paper-bright);
    font-size: clamp(1.35rem, 2.2vw, 1.8rem);
    font-weight: 650;
    line-height: 1.45;
  }
  .advisory-callout {
    margin-top: 2.2rem;
    padding: 1.35rem;
    border: 1px solid #5d584f;
    border-left: 7px solid var(--red);
    background: #22211d;
  }
  .advisory-callout span {
    color: var(--amber);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .advisory-callout p {
    margin: 0.6rem 0 0;
  }
  .enterprise-proof {
    margin-top: 1.7rem;
    font-size: 0.86rem;
  }
  .enterprise-proof a {
    margin-top: 0.55rem;
    display: block;
    color: var(--paper-bright);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.62rem;
    font-weight: 700;
    text-decoration-color: var(--red-bright);
    text-underline-offset: 5px;
    text-transform: uppercase;
  }
  .expertise-card {
    grid-column: 1 / -1;
    margin-top: 1rem;
    display: grid;
    grid-template-columns: 0.45fr 1.55fr;
    border-top: 1px solid #676158;
  }
  .expertise-card > p {
    margin: 0;
    padding: 1.25rem 0;
    color: var(--amber);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.63rem;
    text-transform: uppercase;
  }
  .expertise-card ul {
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    list-style: none;
  }
  .expertise-card li {
    padding: 1.25rem;
    border-right: 1px solid #676158;
    border-bottom: 1px solid #676158;
    color: #d4cfc5;
    font-size: 0.86rem;
  }
  .expertise-card li::before {
    content: "△";
    margin-right: 0.7rem;
    color: var(--red-bright);
    font-family: "IBM Plex Mono", monospace;
  }

  .connect-chapter {
    min-height: 78svh;
    padding: clamp(7rem, 11vw, 9rem) max(1rem, calc((100vw - 1160px) / 2));
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    align-items: center;
    gap: clamp(3rem, 8vw, 7rem);
    background: var(--paper);
  }
  .connect-chapter h2 {
    max-width: 540px;
    font-size: clamp(3.2rem, 5.5vw, 5.4rem);
    letter-spacing: -0.06em;
    line-height: 0.93;
  }
  .connect-lede {
    max-width: 510px;
    margin-top: 1.4rem;
    color: #6b655c;
    line-height: 1.65;
  }
  .link-grid {
    border-top: 1px solid var(--ink);
  }
  .link-grid a {
    min-height: 90px;
    padding: 1rem;
    display: grid;
    grid-template-columns: 48px 1fr auto;
    align-items: center;
    gap: 1rem;
    border-bottom: 1px solid var(--ink);
    color: var(--ink);
    text-decoration: none;
    transition:
      background 180ms ease,
      color 180ms ease,
      padding 180ms ease;
  }
  .link-grid a:hover {
    padding-inline: 1.4rem 0.6rem;
    background: var(--red);
    color: white;
  }
  .link-grid small {
    color: var(--red);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.61rem;
  }
  .link-grid a:hover small {
    color: var(--amber);
  }
  .link-grid strong,
  .link-grid i {
    display: block;
  }
  .link-grid strong {
    font-family: "Bodoni Moda", Didot, serif;
    font-size: 1.8rem;
    font-weight: 500;
  }
  .link-grid i {
    margin-top: 0.15rem;
    color: var(--muted);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.61rem;
    font-style: normal;
  }
  .link-grid a:hover i {
    color: #f4c9c4;
  }
  .link-grid b {
    font-size: 1.35rem;
    font-weight: 400;
  }

  footer {
    min-height: 98px;
    padding-inline: max(1rem, calc((100vw - 1160px) / 2));
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 2rem;
    border-top: 1px solid #4e4a43;
    background: var(--ink);
    color: #918b81;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.61rem;
    letter-spacing: 0.055em;
    text-transform: uppercase;
  }
  footer p:last-child {
    justify-self: end;
  }
  footer a {
    color: var(--paper-bright);
    text-underline-offset: 5px;
    text-decoration-color: var(--red);
  }

  .motion-ready [data-reveal] {
    opacity: 0;
    transition:
      opacity 750ms ease,
      transform 900ms cubic-bezier(0.16, 1, 0.3, 1),
      clip-path 900ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .motion-ready [data-reveal="left"] {
    clip-path: inset(0 100% 0 0);
    transform: translateX(-45px);
  }
  .motion-ready [data-reveal="right"] {
    clip-path: inset(0 0 0 100%);
    transform: translateX(45px);
  }
  .motion-ready [data-reveal="up"] {
    transform: translateY(45px);
  }
  .motion-ready [data-reveal].is-visible {
    opacity: 1;
    clip-path: inset(0);
    transform: translate(0);
  }

  :global(a:focus-visible),
  :global(button:focus-visible),
  :global(input:focus-visible) {
    outline: 3px solid var(--amber);
    outline-offset: 4px;
  }

  @keyframes shutter-left {
    to {
      transform: translateX(-102%);
    }
  }
  @keyframes shutter-right {
    to {
      transform: translateX(102%);
    }
  }
  @keyframes slate-out {
    to {
      opacity: 0;
      transform: translate(-50%, -53%) scale(0.98);
    }
  }
  @keyframes sequence-exit {
    to {
      visibility: hidden;
    }
  }
  @keyframes marquee-drift {
    to {
      transform: translateX(-8vw);
    }
  }
  @keyframes scan {
    0% {
      top: -8%;
      opacity: 0;
    }
    12%,
    82% {
      opacity: 0.75;
    }
    100% {
      top: 108%;
      opacity: 0;
    }
  }
  @keyframes signal-pulse {
    50% {
      box-shadow: 0 0 0 8px rgba(239, 116, 109, 0.05);
    }
  }
  @keyframes scroll-line {
    0%,
    100% {
      transform: scaleY(0.28);
    }
    50% {
      transform: scaleY(1);
    }
  }

  @media (max-width: 980px) {
    .mission-nav {
      grid-template-columns: auto 1fr auto;
    }
    .mission-nav nav {
      display: none;
    }
    .hero-cinematic {
      min-height: auto;
      padding-block: 9rem 6rem;
    }
    .hero-stage {
      grid-template-columns: 1fr;
    }
    .proof-ribbon {
      grid-template-columns: 1fr 1fr;
    }
    .proof-ribbon > * {
      border-bottom: 1px solid #8e887c;
    }
    .proof-ribbon > *:nth-child(2n) {
      border-right: 1px solid #8e887c;
    }
    .offer-heading,
    .offer-grid {
      grid-template-columns: 1fr;
    }
    .offer-heading .eyebrow {
      margin-bottom: -0.5rem;
    }
    .offer-grid article {
      min-height: 360px;
    }
    .portrait-stage {
      width: min(620px, 100%);
    }
    .book-layout,
    .notes-chapter,
    .work-layout,
    .connect-chapter {
      grid-template-columns: 1fr;
    }
    .book-stage {
      width: min(460px, calc(100% - 1rem));
    }
    .notes-chapter {
      min-height: auto;
    }
  }

  @media (max-width: 680px) {
    :global(html) {
      scroll-padding-top: 4.2rem;
    }
    .mission-nav {
      min-height: 62px;
    }
    .wordmark {
      font-size: 0.68rem;
    }
    .nav-cta {
      min-height: 38px;
      padding-inline: 0.7rem;
      font-size: 0.6rem;
    }
    .nav-cta > span {
      display: none;
    }
    .hero-cinematic {
      padding: 7.6rem 1rem 5.5rem;
    }
    .hero-marquee {
      display: none;
    }
    .hero-credit {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.3rem;
    }
    h1 {
      font-size: clamp(3.45rem, 17vw, 5.2rem);
    }
    .hero-intro {
      font-size: 1rem;
    }
    .hero-signup {
      margin-top: 1.25rem;
      box-shadow: 6px 6px 0 var(--red);
    }
    .portrait-stage {
      padding: 0.7rem;
      box-shadow:
        8px 8px 0 var(--amber),
        -8px -8px 0 rgba(207, 47, 33, 0.7);
    }
    .frame-index,
    .portrait-stage figcaption {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.25rem;
    }
    .scroll-cue {
      display: none;
    }
    .chapter-register span:last-child {
      display: none;
    }
    .proof-ribbon {
      grid-template-columns: 1fr;
    }
    .proof-ribbon > *,
    .proof-ribbon > *:nth-child(2n) {
      min-height: 92px;
      border-right: 1px solid #8e887c;
    }
    .book-chapter,
    .notes-chapter,
    .work-chapter,
    .offer-chapter,
    .connect-chapter {
      padding-inline: 1rem;
    }
    .book-copy h2,
    .notes-heading h2,
    .work-heading h2,
    .connect-chapter h2 {
      font-size: clamp(3.1rem, 14vw, 4.5rem);
    }
    .book-stats {
      grid-template-columns: 1fr;
    }
    .book-stats span {
      min-height: 70px;
    }
    .button {
      width: 100%;
    }
    .notes-cta {
      min-height: 150px;
      grid-template-columns: 1fr auto;
    }
    .notes-cta span {
      grid-column: 1 / -1;
    }
    .expertise-card {
      grid-template-columns: 1fr;
    }
    .expertise-card ul {
      grid-template-columns: 1fr;
    }
    .link-grid a {
      grid-template-columns: 36px 1fr auto;
    }
    footer {
      padding-block: 1.4rem;
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
    footer p {
      margin: 0;
    }
    footer p:last-child {
      justify-self: start;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :global(html) {
      scroll-behavior: auto;
    }
    .opening-sequence {
      display: none;
    }
    .hero-marquee,
    .scan-line,
    .signal-dot,
    .scroll-cue i {
      animation: none;
    }
    *,
    *::before,
    *::after {
      scroll-behavior: auto !important;
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }
</style>
