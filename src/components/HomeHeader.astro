<header class="home-header">
  <a class="home-wordmark" href="#top" aria-label="Vanielle Lee, home">
    <strong>Vanielle Lee</strong>
    <span>Field system / online</span>
  </a>

  <nav aria-label="Homepage navigation">
    <a href="#work">Work</a>
    <a href="#book">Book</a>
    <a href="/blog/">Journal</a>
    <a href="#connect">Connect</a>
  </nav>

  <a class="join-link" href="#newsletter">
    Join the field notes <i aria-hidden="true">↘</i>
  </a>
</header>

<style>
  .home-header {
    position: sticky;
    z-index: 30;
    top: 0;
    width: 100%;
    min-height: 78px;
    padding-inline: max(1rem, calc((100vw - 1240px) / 2));
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: clamp(1rem, 3vw, 3rem);
    border-bottom: 1px solid rgba(25, 25, 21, 0.45);
    background: rgba(251, 248, 239, 0.96);
    color: #191915;
    font-family: "Instrument Sans", sans-serif;
    backdrop-filter: blur(14px);
  }
  .home-wordmark {
    display: flex;
    align-items: baseline;
    gap: 0.85rem;
    color: #191915;
    text-decoration: none;
  }
  .home-wordmark strong {
    font-size: 0.9rem;
    letter-spacing: -0.025em;
  }
  .home-wordmark span {
    color: #cf2f21;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.52rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }
  nav {
    display: flex;
    gap: clamp(1rem, 2.4vw, 2.2rem);
  }
  nav a {
    color: #191915;
    font-size: 0.74rem;
    text-decoration: none;
  }
  nav a:hover {
    color: #cf2f21;
  }
  .join-link {
    min-height: 42px;
    padding: 0.65rem 0.9rem;
    justify-self: end;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: #191915;
    color: #fbf8ef;
    font-size: 0.7rem;
    text-decoration: none;
  }
  .join-link i {
    color: #ef746d;
    font-style: normal;
  }

  @media (max-width: 860px) {
    .home-header {
      grid-template-columns: 1fr auto;
    }
    nav {
      display: none;
    }
  }
  @media (max-width: 560px) {
    .home-header {
      min-height: 66px;
    }
    .home-wordmark span {
      display: none;
    }
    .join-link {
      min-height: 38px;
      padding-inline: 0.7rem;
      font-size: 0.64rem;
    }
  }
</style>
